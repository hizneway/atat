namespace: "{{ environment }}"
bases:
  - ../../../deploy/azure/
resources:
  - namespace.yml
patchesStrategicMerge:
  - envvars.yml
  - ports.yml
  - autoscaling.yml
  - secret_provider.yml
  - azure.yml
